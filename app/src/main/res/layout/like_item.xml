<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- 좋아요 화면의 리사이클러뷰의 아이템 -->
    <!-- 코디 이미지 -->
    <ImageView
        android:id="@+id/like_item_codi"
        android:layout_width="170dp"
        android:layout_height="170dp"
        android:layout_marginLeft="12dp"
        android:layout_marginTop="12dp"
        android:layout_marginRight="12dp"
        android:layout_marginBottom="12dp"
        android:background="#E3E3E3" />

    <!-- 코디 만든 사람의 닉네임 -->
    <TextView
        android:id="@+id/like_item_nickname"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/like_item_codi"
        android:layout_marginStart="12dp"
        android:layout_marginTop="1dp"
        android:text="nickname" />

    <!-- 하트 이미지 -->
    <CheckBox
        android:id="@+id/like_item_btn_like"
        android:layout_width="15dp"
        android:layout_height="15dp"
        android:layout_below="@+id/like_item_nickname"
        android:layout_marginStart="12dp"
        android:layout_marginTop="6dp"
        android:background="@drawable/selector_btn_like"
        android:button="@android:color/transparent"/>

    <!-- 좋아요 숫자 -->
    <TextView
        android:id="@+id/like_item_likeCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/like_item_nickname"
        android:layout_marginStart="30dp"
        android:layout_marginTop="3dp"
        android:layout_marginBottom="30dp"
        android:text="0" />

</RelativeLayout>